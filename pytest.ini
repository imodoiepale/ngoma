[pytest]
# Only the repo's own tests. Vendored node packs under workflows/ ship their own test folders
# (icynodes/tests needs ComfyUI's folder_paths) and are not ours to run here.
testpaths = tests
norecursedirs = workflows node_modules .git .venv graphify-out packages/studio-ui
