# Copy to .env on the VPS: cp .env.example .env && chmod 600 .env
OPENWA_SESSION_ID=epalle
# openssl rand -hex 24 — this is the ONLY thing between the internet and your WhatsApp
OPENWA_API_KEY=
TZ=Africa/Nairobi
