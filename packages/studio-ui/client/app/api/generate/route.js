export async function POST(request){const endpoint=process.env.RUNPOD_ENDPOINT_ID,token=process.env.RUNPOD_API_KEY;if(!endpoint||!token)return Response.json({message:"Dry-run UI works. Add RUNPOD_ENDPOINT_ID and RUNPOD_API_KEY on the server to submit jobs.",configured:false});const input=await request.json();const response=await fetch(`https://api.runpod.ai/v2/${endpoint}/runsync`,{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({input})});const data=await response.json();return Response.json(data,{status:response.status})}
