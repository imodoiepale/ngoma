<!-- generated by packages/engine/references.py possibilities; edit the catalogue or CURATED, not this file -->
# What a reference makes possible

Every node port in `packages/studio-ui/catalog/nodes.json` that takes an image, a clip or a
track, and the gate a reference collection must pass to feed it. `owned or licensed data`
means `use: data` with `rights: owned|licensed`; `consent` adds a written release for the
person shown; anything with unclear rights is `inspiration only` and reaches a prompt as
fragments, never a port.

| Reference | Node | Port | Makes possible | Gate |
|---|---|---|---|---|
| a mood board of harvested pins or clips | (no node) | — | prompt fragments only: palette, exposure, contrast, subject placement, copy zone, cut rhythm (features_to_fragments) | inspiration only (mood boards) |
| one reference image | `image-hosted` Image (hosted) | `reference` | a hosted-model image that echoes it | owned or licensed data |
| one image to change | `image-edit` Image edit | `image` | add jewellery, props, a new light or a new outfit by instruction | owned or licensed data |
| one photo of the avatar or product | `carousel` Carousel poses | `image` | bulk pose change: a whole consistent photoset from that one photo | owned or licensed data |
| a handful of photos of one character | `dataset` Character dataset | `images` | a 40-image training set and character board | consent |
| 12 or more photos of one person | `refmod-create` RefMod character | `images` | a reusable character file, no training | consent |
| one or more images for `images` | `lora-train` LoRA training | `images` | Plans an ostris/ai-toolkit character LoRA job from an owned, fictional or consented reference collection: rights gate, config.yaml, captions, pod launch command and manifest. Training itself runs on the pod from launch.sh, by a person | consent |
| the target image | `faceswap` Face swap | `image` | everything but the face stays | consent |
| one face photo | `faceswap` Face swap | `face` | identity swap; consented faces only | consent |
| one still | `long-video` Long video (H3) | `image` | a long, extended clip that starts from it | owned or licensed data |
| one still | `image-to-video` Image to video (WAN) | `image` | a moving shot that starts from it | owned or licensed data |
| one still of the character | `motion-transfer` Motion transfer (SCAIL) | `image` | who performs the motion | owned or licensed data |
| a reference clip of the motion | `motion-transfer` Motion transfer (SCAIL) | `motion` | the character moves like the clip | owned or licensed data |
| one photo of the performer | `lipsync` Image and audio to video | `image` | the face that sings or speaks | consent |
| the song or voice track | `lipsync` Image and audio to video | `audio` | audio drives a whole performance, mouth and timing | owned or licensed data |
| a finished clip | `upscale-video` Upscale video | `video` | a sharper, larger master | owned or licensed data |
| an old or damaged photo | `restore` Restore photo | `image` | repair, colourise and sharpen | owned or licensed data |
| a video clip for `video` | `relight` Relight | `video` | Change the light on footage | owned or licensed data |
| raw clips | `cut` Cut and loop | `video` | trimmed, looped and picked | owned or licensed data |
| a video clip for `background` | `motion-graphics` Motion graphics | `background` | Typed text, kinetic type, lower thirds and phone mock-ups from a JSON spec, drawn with Pillow in the brand kit's colours and encoded by ffmpeg; over footage when a clip is wired in. No GPU, no Node | owned or licensed data |
| a finished image | `compositor` Brand compositor | `media` | exact copy, logo and palette laid on top | owned or licensed data |
| a finished clip | `captions` Captions | `video` | burned-in subtitles from the script | owned or licensed data |
| a voiceover or music track | `captions` Captions | `audio` | the soundtrack the subtitles are timed to | owned or licensed data |
| a clip with speech | `transcribe` Transcribe | `media` | timed text to cut, caption or translate from | owned or licensed data |
| an image or a video for `media` | `postiz` Schedule post | `media` | Instagram, TikTok and more through Postiz, as a draft | owned or licensed data |
| an image or a video for `media` | `whatsapp-status` WhatsApp Status | `media` | Through OpenWA, after approval | owned or licensed data |
| an image or a video for `media` | `export` Export files | `media` | Masters and manifest for the client | owned or licensed data |
| a pose or depth reference | `krea2-t2i` Krea 2 realism image | `pose` | composition control for the generation | owned or licensed data |
| a character sheet or one clear photo | `h3-reference-image` H3 reference image | `identity` | Picture 1: who the still is of | consent |
| outfit, pose and room photos | `h3-reference-image` H3 reference image | `references` | Pictures 2 to 5: what the still keeps around them | consent |
| one photo | `character-sheet` Character sheet | `image` | four views of the character, ready for H3 references | consent |
| one photo of the room or background | `consistent-room` Consistent room | `room` | the same room across every angle and scene | owned or licensed data |
| a pose or edge map | `zimage-controlnet` Z-Image base + turbo | `control` | ControlNet composition for the generation | owned or licensed data |
| one photo of the person to dress | `wardrobe` Change outfit | `image` | the body that keeps its face, pose and room | owned or licensed data |
| photos of the garments | `wardrobe` Change outfit | `clothes` | clothes-only change; nothing else in the frame moves | owned or licensed data |
| the target body shot | `klein-headswap` Head swap (no LoRA) | `image` | the pose, outfit and room that keep their place | consent |
| one face photo | `klein-headswap` Head swap (no LoRA) | `face` | identity onto a body; the person must have signed a release | consent |
| one reference image | `klein-i2i` Recreate an image (Klein) | `image` | the same image recreated with the character as subject | consent |
| one or more images for `image` | `edit-anything` Edit anything (Qwen AIO) | `image` | Phr00t Rapid AIO Qwen edit with Z-turbo upscale | owned or licensed data |
| one or more images for `image` | `nsfw-surgery` Anatomy repair (SDXL) | `image` | Detect and inpaint broken anatomy on generated images | owned or licensed data |
| an image we own or licensed | `watermark-remove` Remove watermark | `image` | the same image without the mark | owned or licensed data |
| a folder of training images or frames | `caption-dataset` Caption a dataset | `media` | captions for LoRA training | owned or licensed data |
| one still of the character | `wan-animate` Animate a character (WAN) | `character` | full-body or face-only swap onto the driving clip | consent |
| a driving video | `wan-animate` Animate a character (WAN) | `video` | the motion and timing the character follows | owned or licensed data |
| one still of the character | `motion-control` Motion control (SCAIL 2) | `character` | who performs the motion | consent |
| a reference clip of the motion | `motion-control` Motion control (SCAIL 2) | `video` | motion lifted from the clip onto the character | owned or licensed data |
| a source clip | `wan-lora-faceswap` Video face swap (WAN LoRA) | `video` | the performance that keeps its motion under a new face | owned or licensed data |
| one or more images for `candidates` | `pick` Your pick | `candidates` | Choose the keepers from the candidates; nothing downstream runs until you do | owned or licensed data |
| a video clip for `candidates` | `pick-video` Your pick (video) | `candidates` | Choose the clips to keep | owned or licensed data |

49 rows: 14 need consent, 34 need owned or licensed data, 1 inspiration only.
