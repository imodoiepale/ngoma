# E03 Original workflow packs

Proposal generated by `packages/strategy/proposals.py` from `brands/_business/ideas.yaml`, the studio template and the pod model plan. Do not edit by hand.

> Planning estimates, not forecasts. Client counts are scenarios; image throughput is measured (444 images per GPU-hour), video throughput and the GPU rate are assumptions until a live run replaces them.

**Family:** Education and community  
**Buyer:** ComfyUI users (units = packs sold)  
**Offer:** Our own tested workflows with setup guides  
**Price:** $39 per client per month (per unit, see buyer)

## Why it sells

Icekiub and LoRAtech monetise packs; our epalle-nodes pack is original.

## How we deliver it

Studio template: [`e03-original-workflow-packs.studio.json`](../../brands/_templates/workflows/e03-original-workflow-packs.studio.json). Diagram: [e03.workflow.html](../diagrams/ideas/e03.workflow.html).

| Step | Runs on | Detail | Gate |
|---|---|---|---|
| Brief | you provide |  | — |
| Register workflow pack | studio code | `packages/library/tools/register_workflow.py` | — |

100% of the working steps run today.

## Numbers

Cost to serve one client a month: **$6** = 0 GPU-hours × $1.64/h + $0 APIs + 0.25 h labour × $15/h + 5% payment fees.

| Scenario | Clients | Monthly revenue | Monthly cost | Profit | Margin | Setup payback |
|---|---|---|---|---|---|---|
| low | 10 | $390 | $77 | $313 | 80% | 0.5 mo |
| base | 60 | $2,340 | $362 | $1,978 | 84% | 0.1 mo |
| high | 200 | $7,800 | $1,160 | $6,640 | 85% | 0.0 mo |

Setup budget $150. Fixed monthly spend $20. First cash in about 21 days.

## What is not ready

- Nothing structural. Every step has a backend and every model it names has a source; it still needs a live run.
- No step has been run end to end on a GPU for this offer yet. Queued is not success.

## Decisions on the gaps

- 2026-09-20: `register-workflow` is now a catalogue step on packages/library/tools/register_workflow.py (attribution and sha256 into workflows/manifest.json). The engine does not execute this python step yet (runner LOCAL_STEPS); it is run from the command line.

## First 30 days

1. **Week 1: prove it.** Run the template on the pod with owned or fictional inputs; record real GPU minutes and replace the 0 GPU-hour estimate.
2. **Week 2: pilot.** One client at a reduced price in exchange for a case study and permission to show results.
3. **Weeks 3-4: sell.** Pitch comfyui users with the pilot; target the low scenario (10 clients) before scaling.
4. **Automate** the recurring delivery with the studio canvas and the dry-run plan check.

## Risks and rules

- Risk: Free alternatives on Discord.
- Compliance: Never bundle bought packs (Icekiub etc.).

## Run it

```bash
uv run --with pyyaml packages/strategy/workflow_author.py --idea E03 --client ongea-pesa
```
