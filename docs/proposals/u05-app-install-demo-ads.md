# U05 App install demo ads

Proposal generated by `packages/strategy/proposals.py` from `brands/_business/ideas.yaml`, the studio template and the pod model plan. Do not edit by hand.

> Planning estimates, not forecasts. Client counts are scenarios; image throughput is measured (444 images per GPU-hour), video throughput and the GPU rate are assumptions until a live run replaces them.

**Family:** UGC ads  
**Buyer:** Mobile app startups  
**Offer:** Voice-over phone-mockup demos in 3 lengths  
**Price:** $1,000 per client per month

## Why it sells

ArtificialQuotient 'Hyperframes + Claude motion graphics' (18k).

## How we deliver it

Studio template: [`u05-app-install-demo-ads.studio.json`](../../brands/_templates/workflows/u05-app-install-demo-ads.studio.json). Diagram: [u05.workflow.html](../diagrams/ideas/u05.workflow.html).

| Step | Runs on | Detail | Gate |
|---|---|---|---|
| Brief | you provide |  | — |
| Motion graphics | studio code | `packages/video/motion_graphics.py` | — |
| Voiceover | studio code | `packages/voice/tts.py` | — |
| Export files | studio code | `packages/compositor/compositor.py` | — |

100% of the working steps run today.

## Numbers

Cost to serve one client a month: **$223** = 2 GPU-hours × $1.64/h + $20 APIs + 10 h labour × $15/h + 5% payment fees.

| Scenario | Clients | Monthly revenue | Monthly cost | Profit | Margin | Setup payback |
|---|---|---|---|---|---|---|
| low | 1 | $1,000 | $273 | $727 | 73% | 0.3 mo |
| base | 4 | $4,000 | $943 | $3,057 | 76% | 0.1 mo |
| high | 10 | $10,000 | $2,283 | $7,717 | 77% | 0.0 mo |

Setup budget $200. Fixed monthly spend $50. First cash in about 14 days.

## What is not ready

- Nothing structural. Every step has a backend and every model it names has a source; it still needs a live run.
- No step has been run end to end on a GPU for this offer yet. Queued is not success.

## Decisions on the gaps

- 2026-09-20: `hyperframes` and `phone-mockup` both resolve to the motion-graphics node, still a declared gap (packages/video/motion_graphics.py). The voice-over is bound; the demo cannot ship without the renderer.
- 2026-09-20 (W6): bound to packages/video/motion_graphics.py; `background.phone: true` with a `screen` image draws the phone mock-up and the typed lines run over it. The demo ships from the app screenshots and the voiceover.

## First 30 days

1. **Week 1: prove it.** Run the template on the pod with owned or fictional inputs; record real GPU minutes and replace the 2 GPU-hour estimate.
2. **Week 2: pilot.** One client at a reduced price in exchange for a case study and permission to show results.
3. **Weeks 3-4: sell.** Pitch mobile app startups with the pilot; target the low scenario (1 clients) before scaling.
4. **Automate** the recurring delivery with the studio canvas and the dry-run plan check.

## Risks and rules

- Risk: Needs real screen recordings.
- Compliance: Show real app behaviour.

## Run it

```bash
uv run --with pyyaml packages/strategy/workflow_author.py --idea U05 --client ongea-pesa
```
